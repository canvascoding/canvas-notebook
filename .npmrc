# Only fail on critical vulnerabilities.
# Remaining high/moderate advisories are known and accepted:
#   xlsx (1108110, 1108111) - no upstream fix available; only reachable by authenticated users
#   esbuild (1102341)       - drizzle-kit fix would require a destructive downgrade (0.31 → 0.18)
audit-level=critical
