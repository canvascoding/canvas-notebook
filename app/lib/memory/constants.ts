/** Reserved system identity for automatic memory reviews; never exposed as a chat agent. */
export const MEMORY_MANAGER_AGENT_ID = 'memory-manager';

export const MEMORY_MANAGER_AGENT_NAME = 'Memory Review Agent';
