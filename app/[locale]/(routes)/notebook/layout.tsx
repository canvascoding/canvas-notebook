export const metadata = {
  title: 'Notebook - Canvas Notebook',
  description: 'Notebook editor',
}

export default function NotebookLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
