export const metadata = {
  title: 'Canvas Chat',
  description: 'Canvas Chat fullscreen interface',
}

export default function ChatLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}