# App Configuration
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0
BASE_URL=http://localhost:3000
BETTER_AUTH_BASE_URL=http://localhost:3000

# Auth configuration (better-auth)
BETTER_AUTH_SECRET=change-me-generate-with-openssl-rand-base64-32
ALLOW_SIGNUP=false

# Optional one-time bootstrap admin on startup
BOOTSTRAP_ADMIN_EMAIL=admin@example.com
BOOTSTRAP_ADMIN_PASSWORD=admin
BOOTSTRAP_ADMIN_NAME=Administrator

# Workspace Configuration
WORKSPACE_DIR=/data/workspace
SQLITE_PATH=/data/sqlite.db

# Terminal Configuration
TERMINAL_IDLE_TIMEOUT=1800000
MAX_TERMINALS_PER_USER=3
SHELL=/bin/bash

# Media & Previews
MEDIA_CACHE_PATH=/tmp/canvas-media-cache
FFMPEG_PATH=ffmpeg

# AI Models (optional)
# CLAUDE_API_KEY=
# GEMINI_API_KEY=
# OPENAI_API_KEY=
# OPENROUTER_API_KEY=
# OLLAMA_API_KEY=

# Optional Docker runtime helpers
AI_CLI_AUTO_INSTALL=true
OLLAMA_CLI_AUTO_INSTALL=true
OLLAMA_MODELS=/ollama/models
